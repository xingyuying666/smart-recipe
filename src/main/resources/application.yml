spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

openai:
  # Prefer the OPENAI_API_KEY environment variable. Do not commit API keys.
  api-key: ${OPENAI_API_KEY:}
  model: ${OPENAI_MODEL:gpt-5.6-luna}
  timeout-seconds: 45
